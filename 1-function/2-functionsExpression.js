//Function in Js
//!Q2 - What is Function Expression?

const plus = function (num) {
    return num + 7;
};

//? this is a simple function expression. the function itself called anynomous function cuz it doesn't have a name and it introduced as an varaible

console.log(square(5));
